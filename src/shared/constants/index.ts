export const steps = ["location", "checkin"];

export const flexColumnClass = `flex flex-col`;
export const flexCenterClass = "flex";
const currentDate = new Date();

export const initialDateRange = {
    startDate: currentDate,
    endDate: currentDate,
    key: "selection",
};
